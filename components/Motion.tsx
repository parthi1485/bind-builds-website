'use client';
import {useEffect} from 'react';
export default function Motion(){useEffect(()=>{const reduced=window.matchMedia('(prefers-reduced-motion: reduce)').matches;if(reduced)return;const revealEls=[...document.querySelectorAll<HTMLElement>('.reveal')];const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('inView');observer.unobserve(e.target)}}),{threshold:.12,rootMargin:'0px 0px -8% 0px'});revealEls.forEach(el=>observer.observe(el));const onScroll=()=>{const y=window.scrollY;document.documentElement.style.setProperty('--scroll',String(y));const hero=document.querySelector<HTMLElement>('.hero');if(hero){const p=Math.min(y/window.innerHeight,1);hero.style.setProperty('--heroScale',String(1+p*.055));hero.style.setProperty('--heroShift',`${p*24}px`);hero.style.setProperty('--heroFade',String(1-p*.32))}document.querySelectorAll<HTMLElement>('[data-parallax]').forEach(el=>{const r=el.getBoundingClientRect();const p=(window.innerHeight-r.top)/(window.innerHeight+r.height);el.style.setProperty('--parallax',`${(p-.5)*46}px`)})};onScroll();window.addEventListener('scroll',onScroll,{passive:true});return()=>{observer.disconnect();window.removeEventListener('scroll',onScroll)}},[]);return null}
