'use client';
import Link from 'next/link';
import {useState} from 'react';
const nav=[['PROJECTS','/projects'],['PACKAGES','/packages'],['ABOUT','/about'],['CONTACT','/contact']];
function Brand(){return <Link href="/" className="brand brandLockup" aria-label="Bind Builds home"><span className="brandBind">bind</span><span className="brandBuilds">BUILDS</span><span className="brandTag">PLAN • BUILD • DELIVER</span></Link>}
export function Nav(){const [open,setOpen]=useState(false);return <><nav className="nav"><Brand/><div className="links">{nav.map(([n,h])=><Link key={h} href={h}>{n}</Link>)}</div><Link className="cta desktopCta" href="/start-a-project">START A PROJECT ↗</Link><button className="menuButton" aria-label="Open menu" onClick={()=>setOpen(!open)}>{open?'CLOSE':'MENU'}</button></nav><div className={`mobileMenu ${open?'open':''}`}><div>{nav.map(([n,h])=><Link key={h} href={h} onClick={()=>setOpen(false)}>{n}</Link>)}</div><Link className="cta" href="/start-a-project" onClick={()=>setOpen(false)}>START A PROJECT ↗</Link><span className="eyebrow">ARCHITECT-LED CONSTRUCTION STUDIO · CHENNAI</span></div></>}
export function Footer(){return <footer className="footer"><span className="eyebrow">BIND BUILDS · ARCHITECT-LED CONSTRUCTION STUDIO</span><h2>PLANNING A PROJECT?<br/>LET’S START WITH A CONVERSATION.</h2><Link className="cta" href="/start-a-project">START A PROJECT ↗</Link><div className="footerLinks">{nav.map(([n,h])=><Link key={h} href={h}>{n}</Link>)}</div><p className="footerMark">CHENNAI · PLAN • BUILD • DELIVER</p></footer>}
export function Page({kicker,title,children}:{kicker:string,title:string,children:React.ReactNode}){return <><Nav/><header className="pageHero"><span className="eyebrow">{kicker}</span><h1>{title}</h1></header>{children}<Footer/></>}
